apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-server.fullname" . }}
  labels:
    app: {{ include "mock-server.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "mock-server.name" . }}
