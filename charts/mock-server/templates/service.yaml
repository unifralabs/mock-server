apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-server.fullname" . }}
  labels:
    {{- include "mock-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mock-server.selectorLabels" . | nindent 4 }}
